{
  "name": "ecommerce-backend",
  "version": "1.0.0",
  "description": "E-commerce backend with Express and Mongoose",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "generate-mock-data": "node -e 'require(\"./utils/generateMockData\").then(() => process.exit(0)).catch(console.error)'"
  },
  "dependencies": {
    "@faker-js/faker": "^8.0.2",
    "axios": "^1.11.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^8.0.3",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
